<aside class="cn-sidebar">
  <div class="cn-sidebar-contentWrapper">
    <div class="cn-sidebar-top">
      <div class="cn-sidebar-wrapper">
        <a class="navbar__logo" href="{{ '/' | url }}">{{ site.meta.title }}</a>
      </div>
      <nav>
        <a class="cn-sidebar-link" href="{{ '/' | url }}">All notes</a>
        <div class="cn-sidebar-wrapper">TAGS</div>
        {% for tag in collections.tagList %}
        <a
          class="cn-sidebar-link {{ 'active' if tag.path | slugify in page.url }}"
          href="{{ '/tags/' | url }}{{ tag.path | slugify }}"
        >
          {% tagDot tag.name %}
          {{ tag.name }} ({{ tag.count }})
        </a>
        {% endfor %}
      </nav>
    </div>
    <div class="cn-sidebar-bottom cn-sidebar-wrapper">
      Bottom
      <!-- <a class="dark-toggle" href="javascript:void(0)">
        <img src="{{ '/static/img/moon.svg' | url }}" alt="Toggle dark mode" />
      </a> -->
    </div>
  </div>
</aside>
